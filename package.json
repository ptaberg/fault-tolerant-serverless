{
  "name": "fault-tolerant-backend",
  "version": "1.0.0",
  "description": "Fault tolerant backend service",
  "main": "handler.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.1574.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2",
    "serverless": "^4.0.0"
  }
}